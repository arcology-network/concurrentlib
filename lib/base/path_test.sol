// SPDX-License-Identifier: GPL-3.0
pragma solidity >=0.8.0 <0.9.0;
import "./Base.sol";

// contract PathTest {    
//     Base ctrn = new Base();
//     constructor() {
//         ctrn.setByKey(ctrn.uuid(), abi.encodePacked('111111'));
//         ctrn.setByKey(ctrn.uuid(), abi.encodePacked('222222'));
//         require(ctrn.length() == 2);
//         require(keccak256('222222') == keccak256(ctrn.getByIndex(1)));
//     }

//     function call() public  {
//         require(ctrn.length() == 2);
//         require(keccak256('222222') == keccak256(ctrn.getByIndex(1)));
//     }
// }
